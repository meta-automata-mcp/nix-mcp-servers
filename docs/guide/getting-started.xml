<section xmlns="http://docbook.org/ns/docbook"
         xmlns:xlink="http://www.w3.org/1999/xlink"
         xmlns:xi="http://www.w3.org/2001/XInclude"
         version="5.0"
         xml:id="sec-getting-started">
 <title>Getting Started with MCP Servers</title>

 <para>
  This guide will help you get started with MCP Servers.
 </para>

 <section xml:id="sec-gs-installation">
  <title>Installation</title>
  
  <para>
   To install MCP Servers, add it as a flake input to your NixOS or Darwin configuration:
  </para>

  <programlisting>
{
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-unstable";
    mcp-servers.url = "github:aloshy-ai/nix-mcp-servers";
  };
}
  </programlisting>
 </section>

 <section xml:id="sec-gs-configuration">
  <title>Configuration</title>
  
  <para>
   MCP Servers can be configured by importing the appropriate module for your system.
  </para>

  <para>For NixOS:</para>
  <programlisting>
{
  imports = [
    mcp-servers.nixosModules.default
  ];

  services.mcp-clients = {
    enable = true;
    # Additional configuration...
  };
}
  </programlisting>

  <para>For nix-darwin:</para>
  <programlisting>
{
  imports = [
    mcp-servers.darwinModules.default
  ];

  services.mcp-clients = {
    enable = true;
    # Additional configuration...
  };
}
  </programlisting>

  <para>For home-manager:</para>
  <programlisting>
{
  imports = [
    mcp-servers.nixosModules.home-manager # For NixOS
    # OR
    mcp-servers.darwinModules.home-manager # For Darwin
  ];

  services.mcp-clients = {
    enable = true;
    # Additional configuration...
  };
}
  </programlisting>
 </section>
</section> 